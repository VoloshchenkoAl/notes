---
layout: base.njk
title: Блог по front-end розробці
---

<ol class="space-y-12">
  {% for post in collections.post | reversed %}
  <li>
    <p>
      <a class="text-2xl font-black hover:underline" href="{{ post.url }}">{{ post.data.title }}</a>
    </p>
    <p>
      <time class="text-sm opacity-60" datetime="{{ post.date | htmlDateString }}">
        {{ post.date | readableDate }}
      </time>
    </p>
    <ul class="flex gap-2 opacity-80 mb-2">
      {% for tag in post.data.tags | postTags %}
        <li class="text-sm">#{{tag}}</li>
      {% endfor %}
    </ul>
    <p>
      {{ post.data.description }}
    </p>
    <p>
      <a class="text-base text-blue-400 hover:underline" href="{{ post.url }}">Читати</a>
    </p>
  </li>
  {% endfor %}
</ol>
